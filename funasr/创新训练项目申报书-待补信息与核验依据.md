# 创新训练项目申报书待补信息与核验依据

这个文件不给老师看，主要是方便你最后定稿和答辩时有据可依。

## 1. 还需要你亲自补充的信息

下面这些内容我不能替你编，所以在正式草稿里都留了口子：

1. `归属学院 / 所在学院 / 专业班级 / 学号 / 联系电话 / 民族 / 出生年月 / 性别`
2. `项目级别`
如果你还没确定，可以先按“省级创新训练项目”写正文预期成果，之后再改。
3. `所属学科`
建议从你的专业出发填写，例如“计算机类 / 软件工程 / 计算机科学与技术 / 智能科学与技术”等，具体要看学校表述口径。
4. `指导教师承担科研课题情况`
这项必须由你和老师核对后据实填写。
5. `项目组成员信息与最终分工`
我给的是和当前系统实现相匹配的建议分工，你只需要把人名和学号替换进去。
6. `经费额度`
我给了一个比较克制的 2000 元建议版。如果老师希望写 0 元、1000 元或学校有固定上限，可以直接改表格。

## 2. 我已经核实过的项目事实

这些内容不是猜的，是我刚刚根据你本机代码和运行状态核对过的：

1. **PDF 模板栏目**
我已提取出申报表正文要求，核心要写的是：`项目简介`、`立项依据` 六小节、`项目研究进度安排`、`已有基础`、`经费预算`。

2. **当前项目确实是整套系统，不只是 FunASR Demo**
已核对到以下实际目录都存在：
`/Users/rongyicheng/Desktop/layout-voice-factory`
`/Users/rongyicheng/Desktop/minio-backend/springboot-minio`
`/Users/rongyicheng/Desktop/funasr/FunASR`
`/Users/rongyicheng/Desktop/layout-tts-0.22.0/layout`
`/Users/rongyicheng/Desktop/speech_campplus_sv_zh-cn_16k-common`

3. **系统名称与功能入口**
前端头部品牌名是“特辣油语音工厂”，功能入口实际包括：
`录音转文字`
`文字转语音`
`智能纪要`
`实时语音`
`声纹对比`

4. **本机当前在线服务**
我已确认以下端口正在监听或可正常访问：
`8081` 前端
`8080` Spring Boot 后端
`8002` ASR 服务
`8003` XTTS 服务
`8004` 声纹服务
`10095` FunASR 实时 WebSocket
`9000/9001` MinIO

5. **我已实际访问到的健康检查结果**
`http://127.0.0.1:8002` 返回 ASR 服务运行中
`http://127.0.0.1:8003/health` 返回情感与语言列表
`http://127.0.0.1:8004/` 返回声纹比对服务运行中

6. **我已实际读取到的历史数据**
离线 ASR 成功记录：
`historyId=37`
文件 `asr_example.wav`
识别文本：`欢迎大家来体验达摩院推出的语音识别模型。`

离线 ASR 失败记录：
`historyId=35`
`demo.wav`
文件较大，报错 `Read timed out`

TTS 成功记录：
`historyId=9`
参考音频 `neutral_voice.wav`
输入文本：`这是一个用于答辩测试的语音合成样例。`
结果文件大小：`224844` 字节

声纹比对成功记录：
`historyId=13`
样例音频 `speaker1_a_cn_16k.wav` 与 `speaker1_b_cn_16k.wav`
得分 `0.6936`
阈值 `0.25`
判断结果：`同一个人`

智能纪要统计：
当前账号下 `totalNotes=1`
成功纪要 `successNotes=1`
总待办 `totalTodos=1`
发言人档案数 `speakerProfiles=0`
片段数 `totalSegments=0`

## 3. 我已核实过的构建结果

1. 后端在 `/Users/rongyicheng/Desktop/minio-backend/springboot-minio` 执行 `mvn -q -DskipTests package` 成功。
2. 前端在 `/Users/rongyicheng/Desktop/layout-voice-factory` 执行 `npm run build` 成功。
3. 前端构建时有 3 类警告：
`Browserslist` 数据较旧
`::v-deep` 写法已过时
静态资源体积偏大

这些警告不影响“系统可运行”的表述，但很适合写进“尚缺少的条件及解决方法”，会显得项目更真实。

## 4. 正文里我刻意没有乱写的地方

1. 没有替你编老师的课题信息。
2. 没有替你编项目成员姓名、学号、班级。
3. 没有把“发言人识别已经大规模验证”写成既成事实。
因为你当前账号下 `speakerProfiles=0`，说明这部分功能是做出来了，但当前数据样本还没正式配齐。
4. 没有把长音频表现写成“完全稳定”。
因为现有历史里已经能看到长音频超时，这一点最好在申报书和答辩里诚实表述为“后续重点优化方向”。

## 5. 我建议你答辩时重点讲的 6 句话

1. 这个项目不是单点模型调用，而是一个完整的多语音智能处理平台。
2. 我们已经打通了前端、后端、模型服务、数据库和对象存储的完整链路。
3. 项目最有价值的部分不是“能识别”，而是“结果可留存、可追溯、可校正、可导出”。
4. 智能纪要模块不是只输出摘要，而是能生成全文、关键词、待办，并支持发言人片段组织。
5. 当前版本已经完成真实本地联调，但长音频稳定性和发言人样本规模还需要继续优化。
6. 这正是创新训练项目要做的事：在已有原型基础上继续研究、完善和形成规范成果。

## 6. 如果你愿意，我下一步最适合继续帮你的三件事

1. 把正式草稿再压缩成**可直接粘进申报表每个文本框**的短版。
2. 按你和老师的真实信息，把 `待补项` 全部替换掉，做成最终版。
3. 再帮你整理一份 **3 分钟答辩稿 + 老师可能会问的问题**。

