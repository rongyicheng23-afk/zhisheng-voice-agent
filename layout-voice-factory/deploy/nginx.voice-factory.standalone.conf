worker_processes  1;
pid /Users/rongyicheng/Desktop/voice.baconboat/layout-voice-factory/deploy/runtime/nginx.pid;
error_log /Users/rongyicheng/Desktop/voice.baconboat/layout-voice-factory/deploy/runtime/logs/nginx-error.log;

events {
    worker_connections  1024;
}

http {
    default_type application/octet-stream;
    access_log /Users/rongyicheng/Desktop/voice.baconboat/layout-voice-factory/deploy/runtime/logs/nginx-access.log;

    types {
        text/html html htm shtml;
        text/css css;
        text/xml xml;
        application/javascript js;
        application/json json;
        image/svg+xml svg svgz;
        image/png png;
        image/jpeg jpeg jpg;
        image/gif gif;
        font/woff woff;
        font/woff2 woff2;
        application/octet-stream bin;
    }

    sendfile on;
    keepalive_timeout 65;
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_comp_level 5;
    gzip_proxied any;
    gzip_types
        text/plain
        text/css
        text/xml
        application/json
        application/javascript
        application/xml
        application/xml+rss
        application/font-woff
        application/vnd.ms-fontobject
        image/svg+xml;

    server {
        listen 8088;
        server_name localhost 127.0.0.1;

        client_max_body_size 200m;

        root /Users/rongyicheng/Desktop/voice.baconboat/layout-voice-factory/dist;
        index index.html;

        location = /index.html {
            add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate";
            expires -1;
        }

        location ~* \.(?:js|css|png|jpg|jpeg|gif|svg|ico|woff|woff2)$ {
            expires 7d;
            add_header Cache-Control "public, max-age=604800, immutable";
            try_files $uri =404;
        }

        location / {
            try_files $uri $uri/ /index.html;
        }

        location /api/ {
            proxy_pass http://127.0.0.1:18080/api/;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location /user/ {
            proxy_pass http://127.0.0.1:18080/user/;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location /ws/funasr {
            proxy_pass http://127.0.0.1:18080/ws/funasr;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "Upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_read_timeout 3600;
            proxy_send_timeout 3600;
            proxy_buffering off;
        }
    }
}
